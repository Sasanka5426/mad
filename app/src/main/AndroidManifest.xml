<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".adminProfile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UpdateFeeAdmin">

        </activity>
        <activity android:name=".beforeDeleteAdmin"></activity>
        <activity android:name=".AdminHome" />
        <activity android:name=".packagesAdmin" />
        <activity android:name=".addNewPackageAdmin" />
        <activity android:name=".updatePackageAdmin" />
        <activity android:name=".addNewMemberAdmin" />
        <activity android:name=".manageMemberAdmin" />
        <activity android:name=".GymAdmin" />
    </application>

</manifest>